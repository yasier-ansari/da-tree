/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { domains: ['pbs.twimg.com', 'encrypted-tbn0.gstatic.com', 'google.com'], }
}

module.exports = nextConfig
